{
  "name": "@atlas-memory/openclaw",
  "version": "0.2.0",
  "description": "Native OpenClaw cognitive-memory plugin backed by the Atlas AGM/Ripple service.",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.22.3 <23 || >=24.15 <25 || >=25.9"
  },
  "files": [
    "dist/index.js",
    "dist/src",
    "service",
    "openclaw.plugin.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "assemble:service": "node scripts/assemble-service.mjs",
    "prepack": "npm run build && npm run assemble:service",
    "postpack": "node -e \"require('node:fs').rmSync('service',{recursive:true,force:true})\""
  },
  "peerDependencies": {
    "openclaw": ">=2026.7.2"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "install": {
      "minHostVersion": ">=2026.7.2"
    },
    "compat": {
      "pluginApi": ">=2026.7.2"
    }
  }
}
